# Development & test dependencies (installs the core runtime too).
-r requirements.txt

# --- Testing -----------------------------------------------------------------
pytest==8.1.1
pytest-asyncio==0.23.6
pytest-cov==5.0.0
pytest-mock==3.14.0

# --- Linting / formatting / typing ------------------------------------------
ruff==0.3.5
black==24.3.0
mypy==1.9.0
types-redis==4.6.0.20240106
types-PyYAML==6.0.12.20240311

# --- Tooling -----------------------------------------------------------------
pre-commit==3.7.0
