# IncidentIQ backend — core runtime dependencies.
#
# This is the LEAN, fully-runnable dependency set powering the implemented
# vertical slice (alert -> incident -> multi-agent RCA -> remediation + approval).
# It installs cleanly with no native build toolchain and runs with zero external
# services (SQLite + in-process cache + deterministic mock LLM).
#
# Heavy / aspirational dependencies (LangGraph, scikit-learn/Prophet, Kubernetes,
# Kafka, Vault, sentence-transformers, OpenTelemetry, ...) live in
# requirements-optional.txt and are only needed to enable the corresponding
# real integrations.

# --- Web framework -----------------------------------------------------------
fastapi==0.109.2
uvicorn[standard]==0.27.1
python-multipart==0.0.9
pydantic==2.6.4
pydantic-settings==2.2.1

# --- Database (async) --------------------------------------------------------
sqlalchemy==2.0.27
aiosqlite==0.19.0          # default local/test driver (zero external services)
asyncpg==0.29.0            # PostgreSQL driver for staging/production
alembic==1.13.1
greenlet==3.0.3            # required by SQLAlchemy async engine

# --- Cache (optional at runtime; degrades to in-process when unset) ----------
redis==5.0.1

# --- Security ----------------------------------------------------------------
PyJWT==2.8.0               # JWT access tokens (password hashing uses stdlib pbkdf2)

# --- LLM (real path is optional; mock provider needs no key) -----------------
openai==1.14.2

# --- Numerics (embeddings + similarity search) -------------------------------
numpy==1.26.4

# --- Observability -----------------------------------------------------------
structlog==24.1.0
prometheus-client==0.20.0

# --- Utilities ---------------------------------------------------------------
httpx==0.27.0
tenacity==8.2.3
pyyaml==6.0.1
python-dotenv==1.0.1
